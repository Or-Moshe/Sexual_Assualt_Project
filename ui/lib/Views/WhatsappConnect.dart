import 'package:flutter/material.dart';

class WhatsappConnect extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('WhatsApp Connect'),
      ),
      body: Center(
        child: Text('Welcome to WhatsApp Connect'),
      ),
    );
  }
}
